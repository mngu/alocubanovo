{% extends "::layout.html.twig" %}
{% block title %}
  {{ parent() }} - {{ article.title }}
{% endblock %}
{% block body %}

  <h1>{{ article.title }}</h1>
  <p>{{ article.content }}</p>

{% endblock %}